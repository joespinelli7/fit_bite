<%=form_for(@user_food) do |f|%>
	<%=f.label "Edit food"%>
	<%=f.collection_select :food_id, Food.all, :id, :name, {:prompt => true}%><br>
	<%=f.label :servings %>
	<%=f.select :servings,options_for_select(1..10) %><br>
 	<div><%=f.submit "Edit #{@user_food.food.name}"%></div>
<%end%><br>
